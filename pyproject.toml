[project]
name = "demo-site"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/momentum-factor",
    "apps/factor-regression",
    "apps/nn-foundations",
]